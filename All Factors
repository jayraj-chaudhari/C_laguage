//Program to print All factors of a given no
//Jayraj Chaughari
#include<stdio.h>
#include<math.h>
int main()
{
    int n,i;
    printf("Enter a number");
    scanf("%d",&n);
    for(i=1;i<sqrt(n);i++)
    {
        if(n%i==0)
        {
            printf(" %d  %d \n",i,n/i);
        }
    }
}
